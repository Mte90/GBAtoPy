// No imports needed - all functions removed to eliminate dead_code warnings

// Legacy shift helper - removed to eliminate dead_code warning
// Kept for reference if needed in future
// pub fn shift_to_python(reg: u8, shift_type: &gbatopy_disasm::operand::ShiftType, amount: &ShiftAmount) -> String { ... }

// Legacy runtime embedding functions - replaced by direct file embedding in pipeline_cmd.rs
// All functions removed to eliminate dead_code warnings
